#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);
    while(1) {
        stack<char> s;
        string a;
        cin >> a;
        if(a == ".") return 0;
        for(int i = 0; i < a.length(); i++) {
            if(a[i] == '(') {

            }
            else if(a[i] == '[') {

            }
            else continue;
        }
    }
}